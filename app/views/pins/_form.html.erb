<%= form_for @pin, html: {multipart: true } do |f| %>
  <% if @pin.errors.any? %>
    <div class="alert alert-warning" 
      <strong> Oooops. An error has occured.</strong>
        <h5 style= "color:red"; ><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:
        <% @pin.errors.full_messages.each do |message| %>
        <%= message %></h5>
        </br>
      <% end %>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :image %><br>
    <%= f.file_field :image, class: "form-group" %>
  </div>

<div class="form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description, class: "form-group" %>
  </div>
  
  <div class="form-group">
    <%= f.submit submit_text, {class: "btn btn-primary"} %>
  </div>
  </div>
  
<% end %>
